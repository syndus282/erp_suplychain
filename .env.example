DATABASE_URL="file:./dev.db"

# Khóa ký JWT session — bắt buộc phải có giá trị thật khi chạy, KHÔNG dùng giá
# trị mẫu này ở production. Sinh khóa mới bằng: openssl rand -base64 32
AUTH_SECRET="dev-only-change-me-before-deploying-anything-real"
